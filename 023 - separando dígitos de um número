#faça um programa que leia um número de 0 a 9999 e mostre na tela cada um dos dígitos separados.
#write a program that reads a number from 0 to 9999 and displays each of the digits separately.

valor = int(input("Digite um valor inteiro(de 0 a 9999): "))

u = valor // 1 % 10
d = valor // 10 % 10
c = valor // 100 % 10
m = valor // 1000 % 10

print(v1)
print("Sua unidade é {}.".format(u))
print("Sua dezena é {}.".format(d))
print("Sua centena é {}.".format(c))
print("Seu milhar é {}.".format(m))
