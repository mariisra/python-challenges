#refaça o desafio 9, mostrando a tabuada de um número que o usuário escolher, só que agora utilizando um laço for.
#redo challenge 9, showing the table of a number that the user chooses, only now using a for loop.

x = int(input("Digite o número da tabuada que desejar: "))

for c in range(0, 11):
  print(c * x)
